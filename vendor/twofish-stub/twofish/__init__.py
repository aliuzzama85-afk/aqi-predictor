"""Stub for the real twofish package (see README's Windows setup notes)."""


class Twofish:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError(
            "twofish is stubbed out in this environment; install the real "
            "'twofish' package (requires a C compiler) if BKS-keystore support "
            "is ever needed."
        )
